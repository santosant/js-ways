// Vamos dizer que, como um exemplo simples, queremos
// representar a coleção de números: 2, 3, 5, 7 e 11.

//strings (elas podem ter qualquer tamanho, assim podemos armazenar muitos
//dados nelas) e usar "2 3 5 7 11"

var listOfNumbers = [2, 3, 5, 7, 11];

console.log(listOfNumbers[1]);

console.log(listOfNumbers[1 - 1]);
