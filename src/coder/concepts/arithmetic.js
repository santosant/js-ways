const [a, b, c, d] = [5, 10, 15, 20];

const sum = a + b + c + d;
const substr = d - b;
const mult = a * b;
const modulo = a % 2;

console.log(sum, substr, mult, modulo);
//tipos de operandos

//-a operador binário que torna a variável negativa
//
